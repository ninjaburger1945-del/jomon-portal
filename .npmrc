# ConoHa環境最適化設定

# ピアデペンデンシーの警告を表示（エラーにはしない）
legacy-peer-deps=false

# 自動的に古いバージョンをインストール
prefer-offline=true

# npm 8+ の strict-peer-deps デフォルト動作を明示的に無効
engine-strict=false

# オプショナル依存関係をスキップしない
no-optional=false
